services:
  db:
    image: postgres:18-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: swoptape
      POSTGRES_USER: swoptape
      POSTGRES_PASSWORD: swoptape
    volumes:
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
